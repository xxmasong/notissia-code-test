import { PaperContainer } from '@/components/containers';
import { Footer, Header, Sections } from '@/components/modules';

function Main() {
  return (
    <PaperContainer>

    </PaperContainer> 
  );
}

export default Main;
