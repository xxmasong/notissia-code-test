import { FooterWrapper } from '@/styles/styled';
import { Typography } from '@mui/material';

function Footer() {
  return (
    <FooterWrapper>
      <Typography>Rest of page</Typography>
    </FooterWrapper>
  );
}


export default Footer;
